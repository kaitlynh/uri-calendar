# Secrets and team-only files (never commit these)
.env
TEAM-NOTES.md

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# IDE / tooling
.vscode/
.idea/
.claude/

# OS files
.DS_Store
Thumbs.db

# Pipeline output (lives on server, not in repo)
events/*.json
events/*.bak
events/*.good

# Test result logs (kept locally, not committed)
tests/test-results/*.log

# Node (if used for frontend)
node_modules/
