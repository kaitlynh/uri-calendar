import type { Component } from 'solid-js';

const Header: Component = () => {
  return (
    <header class='flex justify-center p-5 bg-[#fed81d] shadow-md'>
      <h1 class='text-bold text-lg'>Events in Uri</h1>
    </header>
  );
};

export default Header;
